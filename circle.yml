## Customize the test machine
machine:
  timezone: UTC
  java:
    version: oraclejdk8

  # Add some environment variables
#  environment:
#   

# CIRCLE_ENV: test


test:
  override:
    - "bash run_tests.sh"
  post:
    - "echo Build succeeded!"
 

## Custom notifications
#notify:
#  channel:
